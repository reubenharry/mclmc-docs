## Characterizing effective sample size

Given a set of samples
the number of samples that would be needed to produce an estimate with the same...

It is common to measure the efficiency of a sampler by its effective sample size (ESS).

characterize the effective sample size (ESS) of samples from a sampler as a 